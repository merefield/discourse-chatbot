  {{#if this.showChatbotButton}}
  <DButton
    @id="chatbot-btn"
    @class="btn-primary chatbot-btn"
    @action={{action (fn this.startChatting this.chat)}}
    @label="chatbot.title_capitalized"
    @icon="robot"
  />
  {{/if}}
